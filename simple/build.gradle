/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn more about Gradle by exploring our samples at https://docs.gradle.org/7.2/samples
 */
plugins {
  id "java"
  id "io.freefair.aspectj.post-compile-weaving" version "6.3.0"
}

java {
  toolchain {
    languageVersion = JavaLanguageVersion.of(17)
  }
}

group "org.example"
version "1.0-SNAPSHOT"

//sourceSets.main.aspectj.srcDir "src/aspectj"
sourceSets.main.java.srcDirs = files("src")
sourceSets.test.java.srcDirs = files("test-src")

repositories {
  mavenCentral()
  google()
}

dependencies {
  implementation 'org.aspectj:aspectjrt:1.9.7'

  // Use JUnit Jupiter for testing.
  testImplementation 'org.junit.jupiter:junit-jupiter:5.8.2'
}

tasks.named('test') {
  // Use JUnit Platform for unit tests.
  useJUnitPlatform()
}